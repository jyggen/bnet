<?php

declare(strict_types=1);

/*
 * This file is part of the Battle.net API Client package.
 *
 * (c) Jonas Stendahl <jonas@stendahl.me>
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 */

namespace Boo\BattleNet\Tests\Apis\{{ namespace }};

use Boo\BattleNet\Apis\{{ namespace }}\{{ className }};
use Boo\BattleNet\Regions;
use Http\Factory\Guzzle\RequestFactory;
use PHPUnit\Framework\TestCase;
use Psr\Http\Message\RequestFactoryInterface;

final class {{ className }}Test extends TestCase
{
    /**
     * @return array<int, array<int, RequestFactoryInterface>>
     */
    public function requestFactoryProvider(): array
    {
        return [
            [
                new RequestFactory(),
            ],
        ];
    }
{% for url, endpoint in endpoints %}

    /**
     * @dataProvider requestFactoryProvider
     */
    public function test{{ endpoint.method|ucwords }}(RequestFactoryInterface $factory): void
    {
        $api = new {{ className }}($factory, new Regions\EU(), 'foobar');
        $request = $api->{{ endpoint.method }}({{ endpoint.argumentValues|raw }});

        self::assertSame({{ endpoint.verb|raw }}, $request->getMethod());
        self::assertSame('application/json', $request->getHeaderLine('Accept'));
        self::assertSame('gzip', $request->getHeaderLine('Accept-Encoding'));
    }
{% endfor %}
}
